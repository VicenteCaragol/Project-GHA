[project]
name = "project-gha"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.0,<4",
    "pytest-cov>=7.1.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0,<9",
]
