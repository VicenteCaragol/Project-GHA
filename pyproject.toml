[project]
name = "project-gha"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.0,<4",
]

[dependency-groups]
dev = [
    "pytest>=8.0,<9",
]
